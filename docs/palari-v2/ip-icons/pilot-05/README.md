# Palari V2 IP-as-logo pilot 05

IDs `025–030`. All six native 1254px opaque candidates pass the 32px identity review and contain exactly six seed apertures. Each remains a strict skill reject; see the per-ID audits.

Files are ordered `025`, `026`, `027`, `028`, `029`, `030` in both contact sheets. The shared prompt contract is documented in the parent `README.md`.
