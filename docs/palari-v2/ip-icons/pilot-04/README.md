# Palari V2 IP-as-logo pilot 04

IDs `019–024`. All six native 1254px opaque candidates pass the 32px identity review. Five have exactly six seed apertures; `palari-023` retains five after its allowed retry. Every candidate remains a strict skill reject; see the per-ID audits.

Files are ordered `019`, `020`, `021`, `022`, `023`, `024` in both contact sheets. The shared prompt contract is documented in the parent `README.md`.
