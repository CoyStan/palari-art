# Palari V2 IP-as-logo pilot 03

IDs `013–018`. All six native 1254px opaque candidates pass the 32px identity review and contain exactly six seed apertures. Each remains a strict skill reject; see the per-ID audit JSON for background, modeling, palette, composition, and identity deviations.

Files are ordered `013`, `014`, `015`, `016`, `017`, `018` in both contact sheets. The shared prompt contract is documented in the parent `README.md`.
