# Palari V2 IP-as-logo pilot 06

IDs `031–036`. All six native 1254px opaque candidates pass the 32px identity review and contain exactly six seed apertures. Each remains a strict skill reject; see the per-ID audits.

Files are ordered `031`, `032`, `033`, `034`, `035`, `036` in both contact sheets. The shared prompt contract is documented in the parent `README.md`.
