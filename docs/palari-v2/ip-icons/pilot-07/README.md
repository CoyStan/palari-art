# Palari V2 IP-as-logo pilot 07

IDs `037–041`. All five native 1254px opaque candidates pass the 32px identity review and contain exactly six seed apertures. Each remains a strict skill reject; see the per-ID audits.

Files are ordered `037`, `038`, `039`, `040`, `041` in both contact sheets. The shared prompt contract is documented in the parent `README.md`.
